# Core LangChain and OpenAI support
langchain==0.2.15
langchain-community==0.2.13
langchain-core==0.2.41
langchain-openai==0.1.23
langchain-text-splitters==0.2.4

# OpenAI + environment loading
openai
python-dotenv

# Gradio interface
gradio

# PDF and OCR handling
pypdf
pdf2image
pytesseract
Pillow

# Data handling
pandas
requests

# HTML rendering (not used in current code, remove if unnecessary)
weasyprint

# Optional dependency for parsing HTML (not used directly in your code)
bs4==0.0.2
